### Examples

#### Run server:

```bash
mix run --no-halt
```

Open `http://localhost:4001` in browser or:

```bash
open http://localhost:4001
```

#### Render file:

```bash
mix render
```

```bash
open index.html
```
